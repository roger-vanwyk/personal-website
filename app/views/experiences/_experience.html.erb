<div class="work-experience-entry">
            <% if @edit_button %>
              <%= link_to "Edit (Order: #{experience.order_listed})", edit_experience_url(experience.id), class: "btn btn-success btn-lg" %>
              <%= link_to "Delete", experience, method: :delete, data: { confirm: "You sure?" }, class: "btn btn-success btn-lg" %>
            <% end %>
            <h3 class="work-experience-title"><a href="<%= experience.link %>" target="blank"><%= experience.name %> <span class="work-experience-link">(
              <% if experience.link.include?("www.") %>
                <%= experience.link.split("www.")[1].capitalize %>
              <% else%>
                <%= experience.link.split("//")[1].capitalize %>
              <% end %>
              )</span></a></h3> 
            <ul>
              <li>
                <div class="work-experience-desc">
                  <figure>
                    <a href="<%= experience.link %>" target="blank"><img src="<%= experience.picture.url %>" class="img-responsive" alt="<%= experience.name %>"></a>
                  </figure>
                  <div>
                    <h4><strong>Position: </strong><%= experience.position %></h4>
                    <ul class="work-exprience-points">
                      <% experience.description.split(",").each do |point| %>
                        <li><%= point %></li>   
                      <% end %>  
                    </ul>
                  </div>
                </div>
                <h4 class="work-experience-date"><%= experience.date %></h4>
              </li>
            </ul>
          </div>  